import React, { ReactElement } from 'react';
import { Button } from '@swapi/ui-components';

const App = (): ReactElement => {
  return (
    <div className="App">
      <Button type="button">Hit da button!!</Button>
    </div>
  );
};

export default App;
